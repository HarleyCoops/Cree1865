# Stage-specific dependencies for Dakota Tinker RL.
# The root requirements.txt is not a lock file. Use runtime readiness checks
# after installing this file, because Tinker and legacy Google clients have
# incompatible protobuf expectations.

tinker==0.27.0
tinker-cookbook==0.5.6
protobuf>=7.36.1,<8
chz>=0.4.0
