# Core dependencies for Dakota language extraction
# This file is a convenience install list, not a lock file. Verify the live
# runtime with the readiness checks before paid extraction or RL runs.

# Anthropic API for Claude Sonnet 4.6+ (primary VLM)
anthropic>=0.124.0

# Google Gemini API for Q&A generation. Do not use the deprecated
# google-generativeai package; it pulls protobuf constraints that conflict with
# current Tinker.
google-genai>=2.18.1

# HTTP requests for API calls
requests>=2.34.2

# Analysis and visualization stack
numpy>=2.5.2
pandas>=3.0.5
matplotlib>=3.11.1

# Image processing for vision tasks
Pillow>=12.3.0
PyMuPDF>=1.28.2
pypdf>=6.16.1

# Dataset publishing and model hub integration
huggingface-hub>=1.28.0

# Environment variable management
python-dotenv>=1.2.3

# Progress bars and utilities
tqdm>=4.70.0

# Weights & Biases for experiment tracking and logging
wandb>=0.28.2
weave>=0.53.6

# Thinking Machines / Tinker integration
# Install RL/Tinker dependencies from requirements-tinker.txt after these base
# dependencies. Current Tinker requires protobuf 6.x, while the legacy
# google-generativeai package still declares a protobuf <6 transitive pin.
